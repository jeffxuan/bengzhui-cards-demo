# 临时效果清单

新版文档中存在引用但未在当前规则数据中定义的名词。开发阶段允许按上下文建立临时别名，但不得伪装成已冻结规则。

## 实施政策

1. 能明确映射到现有效果时，使用 `rules/effect_aliases.json` 记录映射。
2. 无法完全映射时，保留牌面意图，使用最小可玩实现，并将 `provisional: true` 写入内容数据。
3. provisional 效果必须生成调试事件，记录来源文档、临时语义和触发次数。
4. provisional 效果不能阻止保存、回放或继续对局；但内容校验器必须在 CI 中输出清单。
5. 首发公开版本前，所有 provisional 项必须转为正式语义或从启用池移除。

## 当前待处理名词

| 名词 | 出现位置 | 临时处理方向 | 状态 |
| --- | --- | --- | --- |
| 铠甲护体 | W 的技能 | 映射为获得 1 点护甲的防御牌效果 | provisional |
| 战绝 | 国王东征、恩科斯 | 映射为角色可用的攻击牌转换技能 | provisional |
| 不退 | 国王东征 | 映射为防止一次濒死/死亡的防御效果 | provisional |
| 一美元 | Signal 制频、冒险家牌 | 先作为无消耗单体攻击牌别名 | provisional |
| 破斩 | 胖大海激愤 | 映射为范围攻击，伤害值按事件上下文确定 | provisional |
| 稳渡 | BY、相关角色 | 映射为临时被动标记，具体奖励按牌面拆分 | provisional |

此清单不是最终设计合同；任何新发现的未定义名词必须先加入这里，再进入代码。
