# 新版内容导入状态

更新时间：2026-08-28

## 已完成

- 内容目录支持逻辑 `card_id` 与独立 `instance_id`（例如 `slash#001`）双层查找。
- 卡牌元数据字段已纳入目录校验：`suit`、`rank`、`color`、`durability`、`copies`、`provisional`。
- 旧版 80 张逻辑牌在运行时自动补齐兼容元数据，不修改原始牌面语义。
- `provisional` 效果和别名表可以被校验并输出报告。

## 等待原文档

当前工作区和附件缓存中没有可读取的新版 `GAME角色.docx`、`GAME卡牌.docx`、`GAME事件.docx`、`GAME规则(1).docx`。因此以下内容尚未导入：

- 新版角色技能文本和限次条件；
- 全量通用牌、7 个职业牌库、装备耐久；
- 花色、点数及每张副本的实际分配；
- 新版事件牌和事件选择效果。

收到可读取的 DOCX 后，导入顺序为：角色定义 -> 通用牌/装备 -> 职业牌库 -> 事件 -> 内容校验和确定性回放测试。

不得使用猜测内容填充上述字段。无法从文档确定的效果必须使用 `provisional` 并登记到 `docs/PROVISIONAL_EFFECTS.md`。
