# 本地验证记录

## 2026-09-08 · 自动化回归检查

- `scripts/ci/verify.sh` 在 Godot 4.6.3 下完整通过：编译、规则、事务存档、三分辨率 UI、固定导出整局冒烟及 1000 局模拟均无错误、非法命令或死锁。
- 导出冒烟：`winner=3`、`reason=last_survivor`、`commands=146`。
- 1000 局模拟：平均 `122.12` 条命令、`7.03` 轮；`last_survivor` 995 局、`simultaneous_wipe` 5 局。
- 当前仍非发布候选：平衡指数为 Ginger `33.60`、K `35.20`、Maddy `17.60`、Na1 `100.40`、Q `47.60`、Shya `55.20`、Signal `48.40`、Z&C `62.00`，其中 Ginger、Na1、Maddy 超出开发预警区间。

## 2026-09-05 · 第十批确认规则卡牌正式化

- 《近距离射击》《暗杀》《雷暴》《龙息》《气势》《以手拒之》已转为正式数据；正式卡牌数量由 67 提升至 73，剩余 92 张保持 `provisional`。
- 新增范围选择 UI 与确定性规则覆盖：近距离攻击的不可响应边界、完整四人轮首次实际受伤、雷暴四象限、龙息行列，以及气势正交定向移动。
- Godot 4.6.3 规则测试和三分辨率 UI 冒烟通过；100 局模拟平均 121.76 条命令、7.08 轮，99 局最后存活、1 局同时全灭，无死锁或非法命令。新增选择流程可被 AI 正常处理，但 Na1 仍偏强，继续补齐牌库前不进行最终平衡。

## 2026-09-05 · 第九批确认规则卡牌正式化

- 《贯穿长枪》《硫磺火》《狙击》《连射》《瞄准》《透支》《武装》均已转为正式数据；正式卡牌数量由 60 提升至 67，剩余 98 张仍显式标记为 provisional。
- 新增确定性规则覆盖《瞄准》只锁定下一张实际攻击牌的响应窗口、《透支》在手牌不足两张时弃尽现有手牌、治疗后严格低于半血才摸两张，以及《武装》的目标与弃牌前置条件。
- Godot 4.6.3 规则测试与三分辨率 UI 冒烟均通过；100 局模拟平均 121.13 条命令、7.00 轮，99 局最后存活、1 局同时全灭，无死锁或非法命令。此模拟仍显示 Na1 偏强、Maddy 偏弱，继续补全牌库后统一平衡。

## 2026-09-04 · 首批无歧义卡牌正式化

- 26 张已有完整、无歧义效果序列的新版基础牌从 `provisional` 转为正式；新版正式卡牌数量由 8 提升至 34，剩余 131 张仍显式标记为 provisional。
- 《一美元》补全为同一目标连续两次各 1 点普通伤害，随后获得 2 金币。该效果、快速愈合的状态/资源变化、四张元素攻击的状态施加均由新增确定性规则测试覆盖。
- `run_rules_tests.gd`、`run_ui_smoke.gd` 和 `run_simulation.gd -- --matches=100` 均通过。模拟平均 `124.49` 条命令、`7.19` 轮，无死锁、非法命令或无效赢家。
- 这次内容补齐没有解决平衡问题：Na1 `102.04`、Maddy `12.00`、Q `32.00` 等观察值仍超出发布目标，待更多牌和角色语义完成后统一处理。

## 2026-09-04 · 第二批无歧义卡牌正式化

- 《跋涉》、强力壳、花绒戒、不屈之心、《TUSK》《浴血》《不眠》已转为正式数据；新版正式卡牌数量由 34 提升至 41，剩余 124 张仍显式为 provisional。
- 新增规则覆盖验证《跋涉》额外移动，以及三件基础饰品在回合开始时分别提供体力、法力和生命回复。`TUSK`、`浴血`、`不眠`已有实际结算测试，本次追加正式状态断言。

## 2026-09-04 · 第三批无歧义卡牌正式化

- 《无情猛攻》、掘金镐、《破斩》与《破甲射击》已转为正式数据；新版正式卡牌数量由 41 提升至 45，剩余 120 张保持 provisional。
- 新增确定性规则测试：三段伤害必须单独结算；掘金镐只在真实伤害落地后加钱；两张“目标有护甲时伤害加 1”的攻击均在护甲抵消之前获得伤害加成。

## 2026-09-04 · 第四批无歧义装备正式化

- 活木甲、地狱重装、猩红披风与地狱颈环已转为正式数据；新版正式卡牌数量由 45 提升至 49，剩余 116 张保持 provisional。
- 内容目录的“可映射”统计改为读取正式 `provisional` 标记，能够正确识别由专用规则层实现的装备被动。

## 2026-09-04 · 第五批无歧义卡牌正式化

- 《翻找》从 `provisional` 转为正式数据，正式卡牌数量由 49 提升至 50，剩余 115 张仍显式标记为 provisional。
- 新增确定性规则测试：验证该牌先抽取两张牌，再请求使用者精确选择两张弃置，并在选择后正确恢复效果结算。
- `run_rules_tests.gd`、`run_ui_smoke.gd` 与 `run_simulation.gd -- --matches=100` 均通过；模拟平均 `122.91` 条命令、`7.08` 轮，99 局最后存活胜利、1 局同时全灭，无非法命令或死锁。

## 2026-09-04 · 第六批无歧义装备正式化

- 《仪式匕首》从 `provisional` 转为正式数据，正式卡牌数量由 50 提升至 51，剩余 114 张仍显式标记为 provisional。
- 新增规则覆盖：基础攻击距离为 1 的职业装备后可攻击距离 2 的目标；击败后生命上限增加 1，且武器耐久重置至最大值。

## 2026-09-05 · 第七批确认规则卡牌正式化

- 《堕落力量》《刺杀》《疾行》《巩固》《铁壁》《血河》已从 `provisional` 转为正式数据；正式卡牌数量由 51 提升至 57，剩余 108 张仍显式标记为 provisional。
- 新增确定性规则覆盖：两次攻击增伤的消耗、攻击体力费用递减、此前两张攻击的额外摸牌、突破护甲上限、使用前护甲分支，以及以自身为中心的 3×3 双层流血。

## 2026-09-05 · 第八批确认规则装备与防御牌正式化

- 《赫尔墨斯之翼》《不动如山》《刺剑》已从 `provisional` 转为正式数据；正式卡牌数量由 57 提升至 60，剩余 105 张仍显式标记为 provisional。
- 新增规则覆盖：赫尔墨斯之翼将免费移动固定替换为 7×7 正交路径；不动如山阻止当前完整轮内的卡牌位移；刺剑只增强本回合移动后的首张攻击牌。
- 新增确定性规则测试覆盖流血/中毒免疫、按实际生命损失加甲、自己回合减伤，以及回合开始的自伤后摸牌。

## 2026-09-04 · v10 无行动点规则收口

- 删除新版对局的行动点字段、每回合重置、市场扣除、确定性快照和 HUD 文案；市场现仅由金币与每回合一次的 `market_bought` 标记限制。
- 保留旧 `extra_action` 效果名的内容校验兼容，供迁移数据与定向测试读取；若旧回放触发该效果，规则层记录 `legacy_effect_ignored`，不再创造行动计数。
- 规则版本升至 10，因此 v9 及更早的中途对局会被保留并提示不兼容。
- `run_rules_tests.gd`、`run_ui_smoke.gd` 与 `run_simulation.gd -- --matches=100` 均通过。模拟平均 `125.95` 条命令、`7.24` 轮，100% 最后存活结束，无死锁、非法命令或无效赢家。
- 该检查点不代表可发布：100 局观察值仍明显失衡，Na1 `93.88`、Maddy `8.00`、Shya `72.00`，必须在发布前单独完成数值与 AI 平衡工作。

## 2026-09-02 · v9 装备实例与雷佑时序

- 装备牌安装时不支付体力或法力，具体实例 ID 从手牌移入对应装备槽；耐久、替换和销毁继续跟踪同一实例。
- Q 先完成正常摸牌，再获得「发动雷佑 / 暂不发动」选择；跳过选项不改变已摸手牌。
- Godot 4.6.3 规则测试与三分辨率 UI 冒烟测试通过，`git diff --check` 通过。

## 2026-09-01 · v7 K「巨脑」与 Ginger「腰裂」规则执行

- Godot 4.6.3 编译、v7 规则、v5 内容、事务存档、三分辨率 UI、1.35 倍文字与导出整局冒烟全部通过。
- K「巨脑」现要求准确弃 1 张，按递归→回响→祷告→本回合零伤害的顺序结算；上一张牌从弃牌区转移而非复制，旧护甲效果已删除。
- Ginger 半血增伤由重复计算两次修正为一次；「腰裂」仅在最终造成正伤害且目标生命更低时失去 2 生命，自伤不会递归。
- K「奇策」和 Ginger「强攻」不会再执行旧版摸牌、额外行动、护甲或体力回复；多步选择完成前 UI 标为开发中且不生成合法命令。
- 1000 局稳定性模拟通过：平均 106.74 条命令、5.88 轮；`last_survivor` 999 局、`simultaneous_wipe` 1 局，无非法行动、死锁或无效赢家。
- 平衡不是本检查点门槛：切断旧技能后 K 与 Ginger 明显偏弱，必须在完成「奇策」「强攻」后重新平衡，当前构建不应标为候选版。

## 2026-09-01 · v6 新版牌池、洗牌与装备费用

- Godot 4.6.3 编译、v6 规则、v5 内容、事务存档、三分辨率 UI、1.35 倍文字和导出整局冒烟全部通过。
- 初始手牌、通用/职业牌堆和市场的旧版 ID 硬断言为 0；同种子牌序一致，不同种子牌序不同，同名副本不再成组位于抽牌端。
- 装备费用回归合法性校验：武器/防具 1 体力，饰品 1 法力；无资源时命令不会生成，使用后正确扣除。
- 1000 局模拟全部在上限内结束：平均 104.30 条命令、5.76 轮，`last_survivor` 1000 局，无非法行动、死锁或无效赢家。
- 平衡尚未达候选版门槛：Q 和 Z&C 偏强，K、Na1 和 Maddy 偏弱。全牌池仍含大量 `provisional` 效果，本检查点只证明稳定性，不是平衡候选版。

## 2026-08-30 · dev.4 花色/点数与技能限次回归

- Godot 4.6.3 编译、v5 规则、事务存档、三分辨率 UI、1.35 倍文字、键盘焦点与导出冒烟全部通过。
- 第一轮职业选择后保证立即抽到带花色/点数的新版副本；手牌、弃牌弹层、市场和公开出牌记录优先显示 `♠ A` / `♥ K` 等身份。
- K、Maddy 和 Signal 文档明示的限次已进入合法性校验与 UI 剩余次数提示；未写限次的技能继续可重复使用。
- 10000 局发布级稳定性模拟全部在 250 命令/20 轮内结束：平均 107.91 条命令、5.93 轮；`last_survivor` 9997 局，`simultaneous_wipe` 3 局。
- 当前角色平衡仍是观察项：Q 与 Z&C 偏强，K、Na1 与 Maddy 偏弱。dev.4 是规则/UI 测试版，不是 42-58 平衡候选版。

## 2026-08-25 · dev.2 v4 规则回归

- Godot 4.6.3 编译、规则、存档、三分辨率 UI 与导出冒烟通过。
- 修复主动玩家在结算中死亡后重复创建弃牌请求的死锁；1000 局固定模拟无非法命令、死循环或无效赢家。
- v4 仅保留 `last_survivor` / `simultaneous_wipe` 胜因，15→11→7 仅在淘汰时崩坠，回合 5/7 决胜伤害持续生效。
- dev.2 模拟输出会记录胜率、出牌伤害、结束轮数、命令数和每名角色的出现次数。当前开发预警范围为 35–65；公开候选版仍需通过 42–58 并完成真人测试。
- dev.2 导出物：Windows x86_64 `BengzhuiCards.exe`，112 MiB，SHA-256 `b3a559864a336d14eb9530b78e2e94381fb94be1d7d984b3909ab7a5a4172d6a`；macOS universal `BengzhuiCards.zip`，69 MiB，SHA-256 `75006434694097d945cdc0cb76a627975a6d4d381d945c14f69c5adbc2fd20a3`。Windows 真机和 macOS 本机整局仍是发布前人工门槛。

## 2026-08-24 · 棋盘键盘操作与界面审计

环境：Godot 4.6.3 stable，Apple M4，规则版本 3，内容版本 2，分支 `codex/public-demo`。

### 自动验证

- `scripts/ci/verify.sh` 完整通过：编译、20 组规则测试、事务存档测试、三分辨率 UI、固定整局导出冒烟及 1000 局模拟均无 Godot 错误或警告。
- 1000 局模拟：平均 6.24 轮、107.69 条命令、2.68 次淘汰；最后存活 848 局，回合上限 150 局，同时全灭 2 局；角色平衡指数 44.0-56.0。
- UI 测试新增棋盘 Tab 焦点、方向键光标、回车确认玩家/空格，以及选择合法目标后的自动聚焦，三个目标分辨率全部通过。

### 界面审计

- 技术审计为 17/20，P0/P1 均为 0；详细结果与已知限制见 `docs/UI_AUDIT.md`。
- 危险红在面板上的对比度由 3.91:1 提升至 4.76:1；主文字 13.02:1、弱化文字 7.12:1，均达到 WCAG AA。
- 自绘棋盘仍不提供逐格屏幕阅读语义，当前版本不得宣称完整屏幕阅读支持。

### 导出验证

| 平台 | 文件 | 大小（字节） | SHA-256 |
| --- | --- | ---: | --- |
| Windows x86_64 | `export/windows/BengzhuiCards.exe` | 117059496 | `20bd9ec1148e5cb6e4d77f1e6de99e096a3ef20b534744fe1139fd3c6ae5aa85` |
| macOS universal | `export/macos/BengzhuiCards.zip` | 72014338 | `f91b4b994a8485adfaf9df8155b7f37487a6c0fe47ff1a4cbe03f226e210d558` |

macOS 包内可执行文件同时包含 `arm64` 与 `x86_64`；从新解包目录完成固定整局：玩家 3 最后存活，113 条命令，退出码 0。Windows 文件确认为 x86-64 PE，但真实 Windows 整局、12 人封闭测试和至少 30 人公开测试仍需外部完成。

## 2026-08-18 · 公开试玩导出刷新

环境：Godot 4.6.3 stable，Apple M4，规则版本 3，内容版本 2，分支 `codex/public-demo`。

### 自动验证

- 发布级 10000 局模拟通过：平均 6.26 轮、107.89 条命令、2.67 次淘汰。
- 结束原因：最后存活 8549 局（85.49%），回合上限 1438 局（14.38%），同时全灭 13 局（0.13%）。
- 角色平衡指数：Q 42.80，K 47.00，Shya 52.44，Ginger 48.76，Z&C 48.88，Na1 48.32，Maddy 54.84，Signal 56.96。
- 伤害统计：单体 288341，范围 32，决胜加成 89481；未发现非法命令、死循环、无胜者或无效赢家。

### 导出验证

- 固定整局导出冒烟通过：`EXPORT_SMOKE_OK: winner=3 reason=last_survivor commands=113`，退出码 0。
- macOS 包内可执行文件通过 `lipo -info`，同时包含 `arm64` 与 `x86_64`。

| 平台 | 文件 | 大小（字节） | SHA-256 |
| --- | --- | ---: | --- |
| Windows x86_64 | `export/windows/BengzhuiCards.exe` | 117055720 | `79e091952f094eac43dd6cdb2094c01eaa9b3965598b856c39e031279b43a807` |
| macOS universal | `export/macos/BengzhuiCards.zip` | 72010335 | `48c308a0cc33f25af8ad87025cd803413b6ae441adab7587abeb9f423e252f76` |

Windows 文件已确认为 x86-64 PE；真实 Windows 整局验证仍需外部 Windows 环境完成。12 人封闭测试和至少 30 人公开测试尚未执行，不能由自动模拟替代。

## 2026-08-17 · 存档恢复与错误反馈硬化

环境：Godot 4.6.3 stable，Apple M4，规则版本 3，内容版本 2。

### 自动验证

- `scripts/ci/verify.sh`：编译、20 组规则测试、事务存档测试、三分辨率 UI 冒烟、固定整局导出冒烟及 1000 局模拟全部通过，无 Godot 错误或警告。
- 存档写入现在先写 `.tmp`，校验后替换并保留 `.bak`回滚路径；损坏 JSON 会保留 `.invalid` 副本，中断写入可从备份恢复。
- 回放结构现在严格校验版本、种子、四人阵容、角色 ID 与命令数组；损坏结构不会触发脚本异常。
- 模拟结果：平均 6.24 轮、107.69 条命令、2.68 次淘汰；最后存活 848 局，回合上限 150 局，同时全灭 2 局；角色平衡指数 44.0–56.0。

### 界面验证

- 损坏存档提示在 1280x720 和 1.35 倍文字下仍保持可见，开始按钮不超出视口。
- 角色详情正文改为可滚动区，超长本地化不会推走主操作。
- UI 冒烟会在退出前恢复原始回放文件，不污染玩家的继续对局入口。

### 导出与外部门槛

本检查点未重新导出安装包；Windows 真机整局和 12/30 人测试仍需外部环境完成。

## 2026-08-05 · 存档与界面硬化检查点

环境：Godot 4.6.3 stable，Apple M4，规则版本 3，内容版本 2。

### 自动验证

- `scripts/ci/verify.sh`：编译、19 组规则测试、三分辨率 UI 冒烟、固定整局导出冒烟及 1000 局模拟全部通过，无 Godot 错误或警告。
- 新增覆盖：回放重建后的确定性快照与完整事件历史一致；旧规则版本、旧内容版本和损坏命令序列会被拒绝且不修改输入文档。
- 新增覆盖：事件牌堆耗尽回收、空事件堆、玩家弃牌堆洗回、双空牌堆、零金币、空手牌及无射程内目标。
- 1000 局模拟：平均 6.24 轮、107.69 条命令、2.68 次淘汰；最后存活 848 局，回合上限 150 局，同时全灭 2 局。
- 角色平衡指数范围 44.0–56.0，符合 42–58 门槛。

### 界面验证

- 1280x720、1920x1080、2560x1440 均通过角色选择和对局 HUD 几何断言。
- 键盘初始焦点、Escape 打开/关闭设置、设置弹层焦点、1.35 倍文字与减少动态效果全部通过。
- 1280x720 在 1.35 倍文字下的手牌带、结束回合按钮和 15x15 棋盘均保持在视口内。
- 本轮桌面可视复核因 macOS 锁屏未重复执行；不替代 2026-08-04 已完成的本机可视检查。

本检查点未重新导出安装包；下方 2026-08-04 的导出物哈希仍是最新发布基线。

## 2026-08-04 · 0.1.0-dev.1

环境：Godot 4.6.3 stable，Apple M4，规则版本 3，内容版本 2。

### 自动验证

- `scripts/ci/verify.sh`：编译、16 组规则测试、三分辨率 UI 冒烟、固定整局导出冒烟及 1000 局模拟全部通过。
- 10000 局发布模拟：平均 6.26 轮、107.89 条命令、2.67 次淘汰。
- 结束原因：最后存活 8549 局（85.49%），回合上限 1438 局（14.38%），同时全灭 13 局（0.13%）。
- 角色平衡指数：Q 42.80，K 47.00，Shya 52.44，Ginger 48.76，Z&C 48.88，Na1 48.32，Maddy 54.84，Signal 56.96。
- 伤害统计：单体 288341，范围 32，决胜加成 89481；无非法命令、死循环、无胜者或无效赢家。

### 界面验证

- 1280x720、1920x1080、2560x1440 均通过自动布局断言。
- 本机可视检查覆盖角色选择、15x15 棋盘、三栏 HUD、手牌带和顶栏；未发现溢出、遮挡或棋盘比例异常。

### 导出物

| 平台 | 文件 | 大小（字节） | SHA-256 |
| --- | --- | ---: | --- |
| Windows x86_64 | `export/windows/BengzhuiCards.exe` | 117036688 | `e8388c60d086df695cfdb82bf388167e6580529b0d13a05e7e5ca943c8ab32e3` |
| macOS universal | `export/macos/BengzhuiCards.zip` | 71992092 | `c13c99fbd9a4f1f07eebef74ddad77af46763f38b70fb8e1ce7b0e10a6f18c56` |

macOS 包内可执行文件已确认同时包含 `arm64` 与 `x86_64`。从新解包目录运行 `--export-smoke` 完成整局：玩家 3 最后存活，113 条命令，退出码 0。

Windows 文件已确认为 x86-64 PE；真实 Windows 整局验证仍是公开候选版的外部阻塞项。真人封闭与公开测试也尚未执行。
## 2026-09-03 Local Checkpoint

- `run_rules_tests.gd`: passed.
- `run_ui_smoke.gd`: passed at the three target resolutions.
- `run_simulation.gd -- --matches=100`: completed with no deadlock, rejected command, or invalid winner; average commands `127.15`.
- Not release-ready: the 100-match observation remains materially unbalanced (`Na1 89.80`, `Maddy 16.00` standardized balance index), and the average command count remains above the release target of `125`.
- Fixed in this checkpoint: retired Signal navigation no longer leaks into the staged kit, Signal Frequency is limited to one use per turn, and Q's end-turn Thunder Guard cannot recursively re-arm itself after a defense result.

## 2026-09-03 · Signal 探混回归

- 新增确定性规则覆盖：Signal 崩坠后保留在原坐标、崩坠区内仍有合法正交移动、每回合首次离开有效区恰好获得 2 金币及摸 2、连续三个回合开始时在崩坠区获得混乱、返回有效区后连续停留计数重置。
- `run_rules_tests.gd` 与 `run_ui_smoke.gd` 通过；`git diff --check` 通过。
- `run_simulation.gd -- --matches=100` 通过：无死锁、非法命令或无效赢家，平均 `127.07` 条命令、`7.44` 轮，全部为最后存活胜利。
- 此结果仅证明稳定性，不能发布：Na1（`89.80`）显著偏强，Maddy（`16.00`）显著偏弱，平均指令数也高于 `125` 的发布目标。

## 2026-09-03 · Maddy 开垦结算

- 「开垦」现在只会在 Maddy 对其他角色造成实际伤害后提供可选入口；确认后依次指定 3 个无角色占据的普通格，于 Maddy 回合结束时用比赛 RNG 随机转化为神异格或财富格。
- Maddy 进入自己开垦的格子会回复 1 点生命并摸 1 张牌。选格、延迟转化、落点收益和每回合限一次均有确定性规则测试。
- UI 使用连续选择弹窗显示坐标；规则测试、三分辨率 UI 冒烟与 `git diff --check` 均通过。
- 100 局模拟仍不适合用于发布平衡判断：AI 尚不会规划开垦地形，Maddy 仍明显偏弱；「宝袋」后半句源文截断，未按猜测实现额外装备栏。

## 2026-09-03 · Z&C 虫刻毒雾

- 「虫刻」首次中毒加层、3x3 毒雾生成、毒雾内回合开始中毒以及确定性快照中的毒雾状态均有规则测试覆盖。
- 毒雾严格在效果所在轮及下一轮有效，共两回合；第三轮开始前清除。
- 规则测试、UI 冒烟与 `git diff --check` 通过。100 局模拟无死锁、非法命令或无效赢家，平均 `125.95` 条命令与 `7.24` 轮。
- 仍不可发布：Na1 `93.88`、Maddy `8.00`、Shya `72.00` 的观察性平衡指数均超出目标范围。
## 2026-09-05 · 第十一批确认规则卡牌正式化

- 《超限手枪》《狂徒思维》《灭杀》《凝视》《风衣》已从 `provisional` 转为正式数据，正式卡牌数量由 86 提升至 91，剩余 74 张保持 `provisional`。
- 为武器射程/费用/近身伤害、指定实例免费、同实例回收加费、跨回合首牌增费和陷阱免疫摸牌增加确定性规则覆盖。
- 规则测试、三种分辨率 UI 冒烟和 100 局模拟通过；100 局平衡观察仍显示 Na1 偏强、Maddy 偏弱，因此该检查点不代表发布候选版。
## 2026-09-05 · 第十二批确认规则卡牌正式化

- 《重刃》《盾斧》《魔力回流》《A+》已从 `provisional` 转为正式数据，正式卡牌数量由 91 提升至 95，剩余 70 张保持 `provisional`。
- 新增攻击/状态伤害区分、对护甲目标增伤、元素牌弃置校验和首张攻击强化的确定性规则测试。
- 规则测试通过；本批未进行数值平衡调整。
## 2026-09-05 · 第十三批确认规则卡牌正式化

- 《刺客匕首》《蛇骸刃》《毒雾瓶》《谷底石》《投石器》已从 `provisional` 转为正式数据，正式卡牌数量由 95 提升至 100，剩余 65 张保持 `provisional`。
- 新增移动后首攻不可响应、实际伤害中毒、回合末区域中毒、生命上限降低免疫和远程武器射程的确定性规则测试。
- 规则测试与 `git diff --check` 通过。

## 2026-09-07 · 第十四批确认规则卡牌正式化

- 《迷途》《防线》《猎杀》《爆破》已从 `provisional` 转为正式数据，正式卡牌数量由 101 提升至 105，剩余 60 张保持 `provisional`。
- 规则测试、三档分辨率 UI 冒烟测试与 100 局固定流程模拟均通过。模拟未出现非法命令、死锁或无效胜者；内容未冻结前，角色胜率只作为观察指标。

## 2026-09-07 · 第十五批装备触发规则正式化

- 《荆棘铠甲》《暗影护符》已从 `provisional` 转为正式数据，正式卡牌数量由 105 提升至 107，剩余 58 张保持 `provisional`。
- 新增反伤递归保护与隐匿后首伤免疫测试；规则测试和 `git diff --check` 通过。

## 2026-09-08 · 本地自动验证

- `scripts/ci/verify.sh` 使用 Godot 4.6.3 完整通过：编译、规则、存档、三档分辨率 UI 冒烟、导出烟测与 1000 局模拟均通过。
- 模拟结果：平均 122.12 条命令、7.03 轮；最后存活 995 局、同时全灭 5 局；未发现非法命令、死循环或无效赢家。
- 平衡观察预警仍存在：Ginger 33.60、Na1 100.40、Maddy 17.60 超出 dev.4 早期预警区间。该结果用于后续 AI/数值调优，不代表功能验证失败或发布候选版。

## 2026-09-10 · 完整牌库检查点

- `rules/cards_new.json` 的 165 张新版逻辑牌均已完成映射，`provisional: false` 为 165，暂存卡牌为 0；611 个花色/点数副本继续使用稳定实例 ID。
- `run_rules_tests.gd` 与 `run_ui_smoke.gd` 通过，后者覆盖 1280x720、1920x1080、2560x1440 三种目标分辨率。
- 固定种子 1000 局模拟通过：平均 122.04 条命令、7.04 轮、2.77 次淘汰；993 局最后存活、7 局同时全灭；未出现非法命令、死锁或无效赢家。
- 模拟发现旧 AI 展示偏置会额外调整 Q、K、Na1 与 Signal 的出牌评分，已删除；去偏置后的 1000 局结果为 Ginger 28.40、K 36.40、Maddy 19.60、Na1 104.40、Q 50.00、Shya 50.80、Signal 45.20、Z&C 65.20。
- 角色强度仍仅为观察指标，不能据此直接改写新版角色文案：自动 AI 尚不会规划 Maddy 的地形收益，也不会权衡 Na1 的少摸换金币与资源转化。下一阶段需结合角色规则、AI 规划能力和真人测试进行平衡，而不是修改模拟权重或臆测削弱数值。

## 2026-09-10 · 实机交互冒烟

- 实机窗口确认加载角色选择、15x15 棋盘、资源与装备栏，以及带花色/点数标识的新版手牌。
- 新增并实机确认回合开始职业选择弹窗：转换职业会明确提示少摸 1 张，保持职业可继续正常摸牌；Q 的雷佑摸牌后选择同样能正常显示。
- 修复回放保存成功时未调度 AI 的界面流程错误，刷新界面与 AI 调度现在始终执行。
- `--export-smoke` 完整对局通过：`winner=3`、`last_survivor`、`commands=146`。另行运行的 100 局模拟无死锁、无非法命令、无无效赢家。
